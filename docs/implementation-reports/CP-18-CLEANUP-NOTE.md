# CP-18 Cleanup Note

در جریان ایجاد Pull Request، Branch کم‌ارزش `noop` به‌اشتباه روی همان SHA مرحله ساخته شد. این Branch هیچ تغییر یا Commit اختصاصی ندارد و باید در پاک‌سازی Branchهای ادغام‌شده حذف شود. Feature واقعی فقط در `work/customer-contract-regression-gate` نگهداری می‌شود.
